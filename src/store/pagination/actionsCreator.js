import { INCREMENT_PAGINATION, DECREMENT_PAGINATION } from './actionsTypes';

export const incrementPagination = () => ({ type: INCREMENT_PAGINATION });
export const decrementPagination = () => ({ type: DECREMENT_PAGINATION });